trigger:
  - develop

pool:
  name: 'CICD_Testing'
  demands:
    - Agent.Name -equals DESKTOP-UIPATH

steps:
# 1. Instalar UiPath CLI en el agente
- task: UiPathInstallPlatform@6
  name: cliStep
  inputs:
    cliType: 'Windows'

# 2. Ejecutar las pruebas unitarias / de integración
- task: UiPathTest@6
  inputs:
    testTarget: 'TestProject'
    orchestratorConnection: 'UiPath_TestConnection'
    projectKey: '9193efec-8988-0000-b111-0b49fcaa3f63'
    testProjectPath: 'project.json'
    environment: 'IniciaME'
    folderName: 'Azure_Testing'
    testReportDestination: '$(Build.ArtifactStagingDirectory)/testResult.xml'
    traceLevel: 'Verbose'

# 3. Empaquetar el proyecto (.nupkg)
- task: UiPathPack@6
  inputs:
    versionType: 'AutoVersion'
    projectJsonPath: '$(Build.SourcesDirectory)/project.json'
    outputPath: '$(Build.ArtifactStagingDirectory)'

# 4. Publicar artefactos
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'
    publishLocation: 'Container'